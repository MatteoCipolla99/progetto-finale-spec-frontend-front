export type Smartphone = {
  title: string;
  category: string;
  brand: string;
  price: number;
  screenSize: number;
  storage: number;
  ram: number;
  battery: number;
  camera: string;
  os: string;
  releaseYear: number;
  description?: string;
  image: string;
};
